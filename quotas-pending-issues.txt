================================================================================
  QUOTAS — PENDIENTES VIGENTES
    Última actualización: 2026-04-21
================================================================================

Este archivo fue limpiado para dejar solo trabajo pendiente real.

El historial de decisiones y lo ya implementado vive en:
- docs/PLANS-AND-PERMISSIONS.md
- docs/QUOTAS-AND-TRAFFIC-PLAN.md
- docs/V1-PRODUCTION-ROADMAP.md
- docs/CHANGELOG.md


## ESTADO REAL HOY

- Phase 1 backend de plans/permissions: lista
- Phase 2 backend de quotas/traffic: lista
- Rollover mensual por período efectivo: listo
- Contrato base Accounts -> SesameFS: decidido
- Auth base para Accounts provisioning: decidido
  - usuario técnico dedicado en org plataforma
  - role `superadmin`
  - API key scope `admin`
  - `Authorization: Token <raw_api_key>`
  - sin sesión humana
  - sin `/api2/auth-token/`

Lo pendiente ya no es el modelo base de quotas. Lo pendiente es cerrar la integración operativa con Accounts y algunos remates de UX/auditoría.


## PENDIENTE — ALTA PRIORIDAD

### 1. BUG: Per-user storage quota no se enforza en uploads (ISSUE-USER-STORAGE-ENFORCE-01)

`CheckStorageQuota` en `internal/traffic/checker.go:42-87` solo lee `organizations.storage_quota`.
No consulta `users.quota_bytes`. El campo `quota_total` (API) / `quota_bytes` (DB) se persiste
y valida al escribir, pero nunca se usa como gate en los pre-checks de upload.

Resultado: un usuario con `quota_total=1GB` dentro de una org de 500GB puede subir hasta
los 500GB de la org, no hasta su límite personal de 1GB.

Per-user traffic (`traffic_upload_quota`, `traffic_download_quota`) SÍ está enforzado.
Solo el storage per-user está roto.

Fix requerido (ver docs/KNOWN_ISSUES.md ISSUE-USER-STORAGE-ENFORCE-01):
- Agregar `userID` a `CheckStorageQuota`
- Leer `users.quota_bytes` cuando `userID != ""`
- Leer counter `user:<orgID>:<userID>` de `storage_counters`
- Aplicar misma lógica hard/soft que el check de org
- Actualizar callers en `HandleUpload`, `UploadFile`, `UploadBlock`
- Test de regresión: user con 1GB cap inside 500GB org debe bloquearse a 1GB


### 2. Formalizar el path M2M interno de Accounts -> SesameFS

Ya está decidida la credencial y ya existe runbook operativo. Lo que falta es cerrar el comportamiento interno del backend para el path máquina-a-máquina.

Pendiente:
- separar internamente cambios manuales de superadmin vs sync M2M de Accounts
- marcar auditoría con `source=accounts`
- registrar actor técnico/credencial de servicio
- agregar idempotencia por `version` o `updated_at`
- rechazar updates viejos o fuera de orden
- dejar trazabilidad clara de provisioning y sync de subscription

Estado actual:
- hoy existen endpoints admin reutilizables
- hoy existe el modelo de API keys endurecido
- hoy existe runbook en `docs/ACCOUNTS-PROVISIONING-RUNBOOK.md`
- falta la formalización del path interno, no la credencial base


### 2. Formalizar el path M2M interno de Accounts -> SesameFS

Ya está decidida la credencial y ya existe runbook operativo. Lo que falta es cerrar el comportamiento interno del backend para el path máquina-a-máquina.

Pendiente:
- separar internamente cambios manuales de superadmin vs sync M2M de Accounts
- marcar auditoría con `source=accounts`
- registrar actor técnico/credencial de servicio
- agregar idempotencia por `version` o `updated_at`
- rechazar updates viejos o fuera de orden
- dejar trazabilidad clara de provisioning y sync de subscription

Estado actual:
- hoy existen endpoints admin reutilizables
- hoy existe el modelo de API keys endurecido
- hoy existe runbook en `docs/ACCOUNTS-PROVISIONING-RUNBOOK.md`
- falta la formalización del path interno, no la credencial base


### 3. ~~Endpoint preview/evaluate para cambios de plan~~ ✅ COMPLETADO

Implementado como `POST /api/v2.1/admin/organizations/:org_id/preview-plan-change/`.
Ver `docs/ACCOUNTS-DASHBOARD-INTEGRATION.md` §5.1 para contrato completo.


### 4. Flujo de invite/create users vía Accounts

Hoy `AddOrgUser` sigue creando usuarios locales en SesameFS y eso no encaja bien con el modelo donde Accounts gobierna identidad.

Hay que cerrar una de estas rutas:
- invite flow: SesameFS inicia el invite, Accounts crea la identidad, OIDC auto-provision completa el alta
- provisioning flow: Accounts crea/provisiona el usuario y luego llama a SesameFS para reflejarlo localmente

Puntos a resolver:
- quién es source of truth del alta de usuario
- cómo se coordina con `max_users`
- qué queda disponible para owner/org-admin dentro de SesameFS


## PENDIENTE — DECISIONES ABIERTAS CON ACCOUNTS

### 5. Identificador de organización en el contrato externo

Falta decidir si Accounts enviará siempre el `org_id` interno de SesameFS o si hace falta resolver por un identificador externo estable.

Opciones abiertas:
- solo `org_id` interno
- `external_org_id`
- ambos


### 6. Persistencia de IDs externos de billing

Falta decidir si conviene guardar en `organizations` alguno de estos campos:
- `accounts_org_id`
- `subscription_id`

Esto afecta trazabilidad, reconciliación y debugging de syncs externos.


### 7. Snapshot pre-downgrade de usuarios activos

Falta decidir si SesameFS debe guardar qué usuarios estaban activos antes de un downgrade a free para poder reactivarlos automáticamente cuando la org vuelva a paid.

Si no se implementa:
- la reactivación sigue siendo manual por owner/admins

Si se implementa:
- hace falta definir formato, lifecycle y reglas de restauración


## PENDIENTE — UX / OPERACIÓN / OBSERVABILIDAD

### 8. Mensajes de error de quota más específicos

Hoy el frontend todavía no distingue bien entre:
- storage quota exceeded
- traffic quota exceeded
- upload traffic quota exceeded
- download traffic quota exceeded
- max users reached

Hace falta devolver y mostrar errores más accionables.


### 9. Visualización de quota user -> org

Falta una UI clara para el panel admin que muestre:
- si el usuario tiene override explícito de quota_total o hereda de la org
- distinguir override explícito vs sin límite propio
- nota: per-user storage enforcement no está implementado aún (ver item 1 / ISSUE-USER-STORAGE-ENFORCE-01)


### 10. UI/admin story para traffic quotas

El backend soporta:
- `traffic_quota`
- `traffic_upload_quota`
- `traffic_download_quota`

Falta decidir si el panel sysadmin tendrá:
- edición manual de emergencia
- solo vista de lectura/debug
- o nada, delegando todo a Accounts


### 11. Audit log de quotas y provisioning

Sigue pendiente persistir y exponer trazabilidad de:
- cambios de quota manuales
- provisioning/sync enviados por Accounts
- actor humano vs actor técnico
- source, timestamp y payload mínimo relevante


### 12. Métricas de drops del recorder

Si el recorder se satura y descarta eventos, hoy no hay suficiente visibilidad operativa.

Hace falta:
- contador/métrica de drops
- logging razonable
- visibilidad para detectar pérdida de telemetría de tráfico


### 13. Dashboard de riesgo y notificaciones

No existe todavía:
- vista de riesgo de quotas por org/usuario
- alertas proactivas por cercanía a límites
- notificaciones email/in-app asociadas a quota o downgrade

Esto es útil, pero no forma parte del núcleo ya implementado de enforcement.


## ORDEN PRACTICO RECOMENDADO

1. Corregir bug per-user storage enforcement (ISSUE-USER-STORAGE-ENFORCE-01) — blocker UX si Accounts ya configura limits per-user
2. Formalizar path M2M interno de Accounts
3. Cerrar el flujo de invite/create users vía Accounts
4. Resolver identificadores externos y snapshot pre-downgrade
5. Completar auditoría, UX fina y observabilidad
